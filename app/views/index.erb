<!DOCTYPE html>

<html lang="en">
  <head>
    <title>List of Shows</title>
  </head>

  <body>
    <h1>List of Shows</h1>

    <ul>
      <% @shows.each do |show| %>
        <li> <%= "#{show.title} (#{show.network})" %> </li>
      <% end %>
    </ul>

  </body>
</html>
